//modelo/atencionModelo.js
const createConnection= require('../base/dbConexion');

//Funcion mostrar atencion
const mostrarAtencion= async (callback)=>{
 try{
  const conexion= await createConnection();
  
 }
}
